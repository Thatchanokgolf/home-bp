[build]
  publish = "public"
  functions = "netlify/functions"

# Pretty API routes -> serverless functions
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[functions]
  node_bundler = "esbuild"
